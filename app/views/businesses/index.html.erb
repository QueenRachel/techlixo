<div class="container-fluid" style="margin-top: 40px;">
  <p id="notice"><%= notice %></p>
  <div class="text-center">
    <h1>Lista de Empresas</h1>
  </div>
</div>

<section class="no-padding" id="portfolio">
    <div class="container-fluid">
        <div class="row no-gutter popup-gallery">
            <% @businesses.each do |business| %>
              <div class="col-md-4 col-sm-6">
                <%= link_to business_path(business), target: 'blank', class: 'portfolio-box' do %>
                  <%= image_tag "portfolio/thumbnails/#{[1,2,3,4,5,6].sample}.jpg", class: 'img-responsive' %>
                  <div class="portfolio-box-caption">
                      <div class="portfolio-box-caption-content">
                          <!-- <div class="project-category text-faded">
                              Category
                          </div> -->
                          <div class="project-name">
                              <%= business.company_name %>
                          </div>
                      </div>
                  </div>
                <% end %>
              </div>
            <% end %>
        </div>
    </div>
</section>

<!-- <table class='table'>
  <thead>
    <tr>
      <th>Fantasy name</th>
      <th>Cnpj</th>
      <th>Email</th>
      <th>Telephone</th>
      <th>Address street</th>
      <th>Address number</th>
      <th>Address complement</th>
      <th>Address neighborhood</th>
      <th>Address city</th>
      <th>Address state</th>
      <th>Address zipcode</th>
      <th>Company name</th>
      <th>Website</th>
      <th>Observation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @businesses.each do |business| %>
      <tr>
        <td><%= business.fantasy_name %></td>
        <td><%= business.cnpj %></td>
        <td><%= business.email %></td>
        <td><%= business.telephone %></td>
        <td><%= business.address_street %></td>
        <td><%= business.address_number %></td>
        <td><%= business.address_complement %></td>
        <td><%= business.address_neighborhood %></td>
        <td><%= business.address_city %></td>
        <td><%= business.address_state %></td>
        <td><%= business.address_zipcode %></td>
        <td><%= business.company_name %></td>
        <td><%= business.website %></td>
        <td><%= business.observation %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->
